window.location.reload
//window.location.href = "<%= pandoc_path %>"
console.log("<%= pandoc_convert_path(@stuff.id, @from, @to) %>");
window.location.href = "<%= pandoc_convert_path(@stuff.id, @from, @to) %>"
